import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#14b3e8",
      main: "#0f9bc7",
      dark: "#061210",
    },
    secondary: {
      light: "#a6a6a6",
      main: "#161616",
      dark: "#232323",
    },
  },
});

export default theme;
